/**
 * Schema / JSON-LD — PLACEHOLDER (foundation).
 *
 * Structured-data generation + validation (Organization, LocalBusiness, FAQ, Article, Breadcrumb). Produces JSON-LD that the connector injects safely (Sophia Stack: via html block).
 *
 * Not implemented yet. See TODO.md and docs/modules.md. When implemented, export
 * `SeoModule` instances here and register them in ../registry.ts (tier-gated).
 */
export {};
