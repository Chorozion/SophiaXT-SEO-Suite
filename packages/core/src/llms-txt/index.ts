/**
 * llms.txt — PLACEHOLDER (foundation).
 *
 * Generate and validate llms.txt for AI-agent discovery. On Sophia Stack this is auto-served today; this module produces proposed content pending extension R2 (configurable llms.txt).
 *
 * Not implemented yet. See TODO.md and docs/modules.md. When implemented, export
 * `SeoModule` instances here and register them in ../registry.ts (tier-gated).
 */
export {};
