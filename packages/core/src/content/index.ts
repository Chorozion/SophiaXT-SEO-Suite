/**
 * Content — PLACEHOLDER (foundation).
 *
 * Tier 2 content generation (planner, article/service/landing drafts, FAQ). DRAFT-ONLY by contract — generated content never auto-publishes. AI provider is read from env; output is untrusted and validated.
 *
 * Not implemented yet. See TODO.md and docs/modules.md. When implemented, export
 * `SeoModule` instances here and register them in ../registry.ts (tier-gated).
 */
export {};
