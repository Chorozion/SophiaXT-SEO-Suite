/**
 * Permissions — PLACEHOLDER (foundation).
 *
 * RBAC + approval-workflow helpers (owner/agency/developer/client). Pure policy logic; enforcement lives in the dashboard middleware.
 *
 * Not implemented yet. See TODO.md and docs/modules.md. When implemented, export
 * `SeoModule` instances here and register them in ../registry.ts (tier-gated).
 */
export {};
