# v1.5 sync checklist — SEO Suite half

> File: docs/v1.5-sync-checklist.md · The SEO half of the joint v1.5 gate (WS6).
> The combined checklist lives in the coordination doc; this is what the SEO Suite
> owns. Both halves must be green to cut v1.5 together.

## Build on shipped surfaces
- [x] R1 — set native `pages.<route>.seo.*` / `model.seo.*`; rendered in `<head>`
      (extension `/optimize-meta`, connector `planMetadataUpdate`).
- [x] R1 — JSON-LD via native `seo.jsonLd[]` (extension `/add-schema`, connector
      `planSchemaAddition`).
- [x] R2 — enumerable versions (`/versions`) + targeted rollback (`/rollback {id}`);
      every write is a **labelled** patch.
- [x] `ctx.ai.embed` — internal-link suggestions (`/suggest-links`, read-only).
- [x] Manifest `requires.sophiaStack ">=1.5.0"`; feature-detect + degrade.

## One-click install (WS4)
- [x] Release-channel descriptor (`release/sophia-seo-suite/channel.json`) +
      release script (zip + sha256).
- [x] Non-destructive install/update/uninstall documented (`docs/release-channel.md`).
- [x] Forward-idempotent settings migration in `activate()` (`migrateSettings`).
- [ ] **Interlock:** confirm the installer's fetch format (repo-source vs zip) with
      the Stack session; align `channel.json` if needed.
- [ ] Publish a GitHub Release `seo-suite-v0.2.0` with the zip asset (run `release`).

## Workarounds to retire as the Stack ships (R3/R4/R5)
- [ ] R3 lands → drop `seo.audit.requested` self-emit; trigger natively.
- [ ] R4 lands → move re-audits to `ctx.jobs` (off inline).
- [ ] R5 lands → mount owner UI via `adminEntry` (off raw routes).

## Screenshots + demo (WS7)
- [ ] SEO half of the joint shots: **SEO audit running** + **applying a fix**
      (title/meta/schema) live in a Stack deployment. Pending a live test against a
      running Stack (cannot capture from the mock). Placeholders:
      `docs/screenshots/`.
- [ ] "See it in action" section in this repo's README once stills exist.

## Cross-cutting (WS8)
- [x] Every write goes through `ctx.site.patch` (validate-before-commit + rollback
      + audit); auth-gated; actor recorded.
- [ ] Live-verify rollback of an applied SEO change inside a Stack deployment.
- [ ] Confirm uninstall leaves `pages.<route>.seo.*` intact (no data loss).
